function boolToWord( boolean ){
    let returnedString = "";
    if (boolean) {
      returnedString = "Yes";
      return returnedString;
    } else {
      returnedString = "no";
      return returnedString;
    }
  }