def print_square(number):
    for row in range[1, number]:
        for column in range[1, number]:
            print("*", end="")
        print()


print_square(2)

# 🦜 Dica: A função print aceita um parâmetro nomeado end que
# pode ser utilizado para imprimir sem a quebra de linha.
# Por exemplo, print(4, end="") e print(2)
# irá imprimir 42 e depois quebrar a linha.
