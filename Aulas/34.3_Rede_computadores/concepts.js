const concepts = {
  DDosAttacks: 'Distrubute denial of service',
  proxies: 'Middle man machine to be used between you and the net. Yout machine -> FireWall -> Proxy -> Internet',
  firewall: '',

  criptography: 'encode a message with a "cifra" so that only those with it can decode it - "paired keys": one encode, the other decodes (the enconder can`t decode, nor the decoder can encode',
  certifications: 'SSL / TLS',

  securityProtocols: {
    SSL_TLS: 'Secure Sockets Layer _ Transport Layer Security ',
    HTTPS: 'protocolo HTTP normal que usa o SSL como uma camada extra de segurança',
    SSH: '',
  },

  Fail2ban: 'O fail2ban é um IPS ( Intrusion Prevention System - Sistema de Prevenção de intrusos). Essa ferramenta, de maneira simples, monitora os logs da rede e, de maneira proativa, ao detectar comportamento suspeito, como diversas requisições de um mesmo IP ou diversas tentativas de login SSH, ele criar regras noiptables , de modo a rejeitar aquele endereço de IP específico por determinado tempo.',


  monitoringPackages: 'traceroute/tracepath _ ping'

}