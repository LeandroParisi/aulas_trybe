const S_singleResponsabilityPrinciple = `Princípio da Responsabilidade Única`

const O_openClosed =` Aberto para extensão, fechado para modificação`

const L_liskovSubstitutionPrinciple = `objetos em um programa devem ser substituíveis por instâncias de suas classes herdeiras sem que isso quebre nada. O que quer dizer, para que esta substituição seja possível, os subtipos devem seguir a interface de um tipo base;

Ou seja, classes herdeiras devem sempre respeitar a interface de suas classes ascendentes! Elas podem especializar comportamentos, mas nunca removê-los! Caso contrário seu código fica cada vez mais difícil de usar, reusar e entender o que se faz!`

const I_interfaceSegregationPrinciple = `Principio da Segregação de Interfaces`

const D_dependencyInversion = `Inversão de Dependências (ou: use composições!)`